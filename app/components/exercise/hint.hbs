<AuButton {{on "click" (fn (mut @popup) true)}}>{{@title}}</AuButton>
<AuModal 
  @modalTitle={{@modalTitle}}
  @modalOpen={{@popup}} 
  @closeModal={{fn (mut @popup) false}} as |Modal|>
  <Modal.Body>
    {{yield}}
  </Modal.Body>
</AuModal>